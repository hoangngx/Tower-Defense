package sample;

import javafx.scene.shape.Rectangle;

public class GameEntity extends Rectangle{
    public GameEntity(int x, int y, int width, int length) {
        super(x, y, width, length);
    }
}
